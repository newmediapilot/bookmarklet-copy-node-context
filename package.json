{
  "name": "bookmarklet-copy-node-context",
  "version": "1.0.0",
  "scripts": {
    "dev": "node scripts/buildBookmarklet.mjs && node scripts/generateIndex.cjs && vite",
    "build": "node scripts/buildBookmarklet.mjs && node scripts/generateIndex.cjs && vite build",
    "compile": "node scripts/buildBookmarklet.mjs",
    "deploy": "npm run build && gh-pages -d dist"
  },
  "type": "module",
  "dependencies": {
    "gh-pages": "6.3.0",
    "terser": "5.48.0",
    "vite": "8.0.16"
  }
}
